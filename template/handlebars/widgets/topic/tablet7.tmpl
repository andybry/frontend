<div class="hot-topics clearfix">
  <h2 class="section-title">{{title}}</h2>
  <div class="teaser-content">
    <ul class="tools">
     {{#each topicMaps}}
        <li><a href="{{url}}" class="more">{{name}}</a></li>
     {{/each}}
    </ul>
  </div> 
</div>
